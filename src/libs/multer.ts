/* import multer from 'multer';
import uuid from 'uuid/v4';
import path from 'path'; */

/*multer.diskStorage({
    destination: 'uploads',
    filename(req,file,cb) => {
        cb(null, uuid())
    }
})*/